# AGENTS.md — NurseLink AI 実装ガイド

このファイルはこのリポジトリで作業するAIエージェント（Claude Code等）向けの作業規約です。
プロジェクトの背景・機能仕様は [README.md](README.md) を参照。ここでは「どう実装するか」「何をしてはいけないか」を定める。

---

## 0. 最優先ルール（スコープ規律）

**これが全てに優先する。**

1. **頼まれたことだけをやる。** ユーザーが指示した範囲を超えて、関連しそうな別の機能追加・リファクタ・「ついでの改善」を勝手に行わない。やった方が良さそうだと思っても、まず提案して確認を取ってから着手する。
2. **指示されていないファイルを変更しない。** 今回のタスクに直接関係するファイル以外（他機能のコード、設定ファイル、READMEやAGENTS.md自体を含む）を編集・削除・リネームしない。ついでに見つけた不具合やコードの気になる点は、直さずに指摘だけして確認を取る。
3. **新しい依存関係・外部サービス・技術選定を勝手に追加しない。** 本ファイル4章の「確定事項」に無いライブラリ・APIを使いたい場合は、先に理由を説明して合意を取る。
4. **大きな設計変更をする前に一度止まる。** ディレクトリ構成の変更、API仕様の変更、DBスキーマの変更など、後戻りしにくい変更は、着手前に方針を短く説明し、明確な合意（またはユーザーからの直接指示）があってから実行する。
5. **不明な点は仮定で進めず質問する。** 特に医療安全・個人情報・スコープに関わる判断は、コード側の都合で勝手に緩めない。

---

## 1. このプロジェクトの性質

- ハッカソン/コンテスト向けMVP。3人チーム（フロント/UX、バックエンド/AI、企画・データ）。
- **診断・医行為は一切行わない。** 問診支援・翻訳支援ツールに徹する。AIが出す内容は常に「看護師が確認・修正して確定する」ドラフトである。
- デモは匿名の模擬データのみ。実個人情報は扱わない。

---

## 2. 実装フェーズ（この順番で進める）

着手する際は、今どのフェーズの作業かをまず明確にすること。フェーズを飛ばして先の機能に手を出さない。

### Phase 1（Must）— 問診パイプライン
⓪同意画面 → ①問診対話画面 → ②問診票確認画面 → （確定と同時に）病院システム（模擬）への登録 ＋ 問診票PDFの自動生成

- ここが完成し、PWAとしてスマホ/タブレットで一通り動く状態を最初のゴールとする。
- ④⑤の通訳系画面より先にこちらを完成させる。
- PDF自動生成はMust。Word/Excelエクスポート（Should、8章参照）はPhase 1の中でも後回しにして良い。

### Phase 2（Should）— 疑似リアルタイム通訳
③通訳呼び出し画面 → ④診察通訳画面

- **真のストリーミング型双方向通訳は実装しない。** 発話が一区切りついたらまとめて翻訳する「疑似リアルタイム」で十分（README参照）。低遅延ストリーミングの実装に時間を使わない。

### Phase 3（Could）— 対応外
実電子カルテ連携、対応言語拡大、手話・筆談モード、真のストリーミング通訳。**指示がない限り着手しない。**

---

## 3. 医療安全・LLMガードレール（必須要件）

症状分類・問診生成・翻訳にLLM（Deepseek API等）を使う実装では、以下を必ず満たすこと。

- **出力は構造化JSON（固定スキーマ）に限定する。** 自由記述の文章で症状分類や問診票を生成させない。スキーマ外の項目・診断や治療方針を示唆する文言が出力に含まれないよう、プロンプトとバリデーション（受信後のパース＋スキーマチェック）の両方で担保する。
- **質問生成は事前定義された質問テンプレートの範囲内で行う。** LLMに自由に問診質問を作らせない。テンプレート＋パラメータ（症状カテゴリ等）で動的に組み立てる方式にする。
- **看護師の確認・修正ステップを迂回する経路を作らない。** ②の確認画面を経由せずに③④以降や病院システム登録・紙版PDF/Word/Excel生成に問診データが渡る実装をしない。**紙版の帳票も、看護師が確定する前のAIドラフト状態からは絶対に生成できないようにする**（病院システム登録と同じ「確定」イベントのみをトリガーにする）。
- LLM呼び出しが失敗・タイムアウトした場合は、黙って何かを推測して埋めない。エラーとして扱い、看護師/患者に分かる形で提示する。

---

## 4. 技術スタック（確定事項）

以下はREADMEで確定した決定事項。変更したい場合は理由を添えて先に相談すること。

| レイヤー | 技術 | 備考 |
|---|---|---|
| フロントエンド | React（PWA） | manifest.json + Service Worker必須 |
| STT | Whisper API | Web Speech APIには依存しない（iOS Safari非対応のため） |
| TTS | クラウドTTS（多言語） | 未確定の場合は着手前に候補を確認 |
| LLM（分類・問診生成・翻訳） | Deepseek API＋翻訳API | 出力は構造化JSON固定（3章参照） |
| バックエンド | Python（FastAPI） | 通訳モードはWebSocket |
| 帳票出力（PDF） | ReportLab | Must。②確定と同時にサーバー側で生成。純Python実装でネイティブ依存（GTK等）が無く、Windows開発機・Fly.io双方でそのまま動く（WeasyPrint等HTML/CSSベースのライブラリはWindowsでの追加セットアップが手間なため採用しない） |
| 帳票出力（Word/Excel） | python-docx / openpyxl | Should。PDFの後に着手 |
| DB／模擬病院システム | Supabase | |
| ホスティング（フロント） | Vercel | |
| ホスティング（バックエンド） | Fly.io | VercelにFastAPI/WebSocketを載せない（実行時間制限のため）。東京(nrt)リージョンを使用 |

---

## 5. モバイル/PWA実装上の必須事項

- **HTTPS必須。マイク権限（getUserMedia）は必ずユーザーの明示的な操作（ボタンタップ等）を起点に要求する。** ページロード時や自動での要求はしない。
- バックグラウンドでのマイク録音・自動再開には依存しない設計にする（iOSでは動作しない）。
- モバイル回線での接続不安定を前提に、API呼び出し・WebSocket接続には再試行とユーザーへのエラー表示を用意する。無言で失敗させない。
- **レスポンシブ対応必須：全画面（⓪〜④）をデスクトップ・スマートフォンの両方で問題なく表示・操作できるようにする。** 特定デバイス幅を前提にした固定ピクセルレイアウトを作らない。ブレークポイントによるレイアウト切り替え（例：スマホは縦積み、デスクトップは余白を活かした横並び等）を各画面で設計する。

---

## 6. プライバシー・同意フロー

- 音声の録音・送信・（必要なら）保存について、⓪同意画面で明示的な同意を得てから①に進む実装にする。同意なしで①以降に進めるルートを作らない。
- デモ用データは匿名の模擬データのみ。実在人物を想起させるダミーデータ（実名風の名前、実在しそうな病歴等)は避ける。

---

## 7. 作業の進め方

- 実装に着手する前に、対象がPhase何か・README/本ファイルのどの決定事項に基づくかを一言で確認してから進める。
- 既存コードに対する変更は、指示された範囲に留める。ついでの整形・リネーム・依存パッケージの更新はしない。
- README.mdと本AGENTS.mdは、内容に影響する意思決定（技術選定変更、スコープ変更等）があったときのみ更新する。ドキュメントの都合で勝手に仕様を変えない。

---

## 8. 開発環境セットアップ

`frontend/`（Vite + React + TS + PWA）と `backend/`（FastAPI）は実装済み。
外部サービス（Supabase / Whisper / Deepseek / TTS）のAPIキーが未設定でも、
すべてモック応答にフォールバックしてローカルで動く（設定すると自動的に本物のAPIを使う）。

**フロントエンド**
```
cd frontend
npm install
npm run dev        # http://localhost:5173
npm run build       # 型チェック＋本番ビルド確認
```

**バックエンド**
```
cd backend
py -3.12 -m venv .venv    # 重要: Python 3.12を使うこと
.venv\Scripts\python -m pip install -r requirements.txt
.venv\Scripts\python -m uvicorn app.main:app --reload --port 8000
```

⚠️ **Python 3.14ではなく3.12を使うこと。** このマシンのデフォルト`python`は3.14だが、`pydantic-core`が3.14向けのビルド済みwheelをまだ配布しておらず、Rustソースビルドに落ちて失敗する（さらにこのプロジェクトパスに日本語が含まれるため、ビルドツールの文字コードエラーで二重に失敗する）。`py -3.12`で明示的に3.12の仮想環境を作ること。Dockerfileも`python:3.12-slim`を使用しており、ローカルと本番のPythonバージョンを揃えている。

**外部サービスへの本接続（任意・設定するだけで自動的に有効化される）**
```
cd backend
cp .env.example .env    # 中身を埋める（各項目の取得元は.env.example内のコメント参照）
```
- Supabase：先に `backend/supabase/schema.sql` をSupabaseダッシュボードのSQL Editorで実行してから `.env` にURL/SECRET_KEY（新しいキー体系。旧称service_role key）を設定する。設定すると `app/store.py` が自動的にインメモリからSupabaseに切り替わる。
- Whisper/TTS：OpenAIのAPIキーを`WHISPER_API_KEY`・`TTS_API_KEY`の両方に設定する（同じ値でよい）。
- Deepseek：`DEEPSEEK_API_KEY`を設定すると症状分類・翻訳(`app/services/llm.py`)が実際のAPIを使うようになる。

**共通の型/スキーマ契約**
- `frontend/src/types/questionnaire.ts` と `backend/app/schemas.py` は同じ問診票スキーマを表す。フィールドを追加・変更する場合は両方を同時に更新すること。
- フロントの画面はいずれも `frontend/src/screens/<screen>/` 配下のみを編集する。ルーティング(`App.tsx`)・共有コンポーネント(`components/`)・共有トークン(`design/tokens.ts`)・型定義・APIクライアントの変更が必要な場合は、まず提案して合意を取ってから行う（0章のスコープ規律）。
- バックエンドの各ルーター(`app/routers/*.py`)は機能ごとに分かれている。他機能のルーターや`app/main.py`のルーター登録部分を編集する必要がある場合も同様に先に相談する。
